{
  "name": "gen-curr-date-frame",
  "version": "1.0.0",
  "description": "Generate a frame with today's date from template frames",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "copyfiles -u 1 \"src/templates/**/*\" \"src/fonts/**/*\" dist && copyfiles run.sh dist && copyfiles .env dist",
    "start": "node dist/index.js",
    "dev": "node --env-file=.env --loader ts-node/esm src/index.ts"
  },
  "keywords": [],
  "author": "Ajinkya Deshpande",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/text-to-svg": "^3.1.4",
    "copyfiles": "^2.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "sharp": "^0.34.5",
    "text-to-svg": "^3.1.5"
  }
}
